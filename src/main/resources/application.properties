spring.application.name=screenmatch
# Db PostgreSQL
#spring.datasource.url=jdbc:postgresql://${DB_URL}/${DB_NAME}
#spring.datasource.username=${DB_USERNAME}
#spring.datasource.password=${DB_PASSWORD}
spring.datasource.url=jdbc:postgresql://localhost:5432/alura_series
spring.datasource.username=postgres
spring.datasource.password=postgres
spring.datasource.driver-class-name=org.postgresql.Driver
hibernate.dialect=org.hibernate.dialect.HSQLDialect
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
# Configurações do Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.format-sql=true
# Forçar o dialeto do Postgres caso a detecção automática falhe [1]
#spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
# log
logging.level.root=INFO
logging.level.com.screenmatch=DEBUG
logging.file.name=logs/app.log